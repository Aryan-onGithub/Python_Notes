import pandas as pd #to short the package name for easy use

df = pd.read_excel("gym.xlsx") #we can read external files with pandas.read operation
print(df)

#sklearn (scikit) package is used in machine learning
#their are packages to merge pdf filse (pypdf2)
#text to speech, notifications, etc
#packages are like mods created by other developers for us to use

#we can install packages by writing
''' pip install packageName'''
#in terminal

# we can also import a sepcific function from a package, for example
from datetime import datetime



