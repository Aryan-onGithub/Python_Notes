#break statement is used to exit a program in specific scope

#continue statement is used to skip one iteration on a loop

'''break''' #syntax
#we can use break in meny ways, few examples are in for loop, while loop, if-else, match cases, etc

'''continue''' #syntax

a = 1
for a in range(5):#syntax
    if(a==2):
        continue   #skips one iteration
    print(a)
    a+=1
